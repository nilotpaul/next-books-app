type AuthorTabtProps = {
  firstName: string;
  lastName: string;
  username: string;
  email: string;
  image: string;
};

const AuthorTab = ({}: AuthorTabtProps) => {
  return <div>AuthorTab</div>;
};

export default AuthorTab;
